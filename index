<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GrowPoint</title>

    <!-- Link para o arquivo CSS -->
    <link rel="stylesheet" href="style.css">

    <!-- Fonte Foundever -->
    <style>
        @font-face {
            font-family: 'Foundever';
            src: url('/Foundever.otf') format('opentype');
        }

        body {
            font-family: 'Foundever', sans-serif;
            margin: 0;
            padding: 0;
        }

        /* Estilo para o cabeçalho com a imagem */
        header {
            width: 100%;
            height: auto;
        }

        header img {
            width: 100%;
            height: auto;
        }

        /* Estilos para o conteúdo principal */
        .container {
            text-align: center;
            padding: 20px;
        }

        h1 {
            font-size: 3em;
            margin-bottom: 10px;
        }

        h2 {
            font-size: 1.5em;
            margin-bottom: 40px;
        }

        /* Estilos para as caixas (links) */
        .menu {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-bottom: 40px;
        }

        .menu a {
            background-color: #4a4af9; /* Cor azul */
            color: white;
            padding: 20px;
            text-decoration: none;
            font-size: 1.2em;
            border-radius: 8px;
            transition: background-color 0.3s, color 0.3s; /* Adicionando transições para fundo e cor do texto */
        }

        .menu a:hover {
            background-color: white; /* Cor de fundo branca */
            color: #4a4af9; /* Cor do texto azul */
        }

        /* Estilo do botão */
        button {
            background-color: #4a4af9;
            color: white;
            font-size: 1.5em;
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #3a3ae6; /* Cor ao passar o mouse */
        }
    </style>
</head>
<body>

    <!-- Barra superior com a imagem GrowSpace -->
    <header>
        <img src="cover.png" alt="GrowSpace Logo">
    </header>

    <!-- Menu de navegação e título -->
    <div class="container">
        <h1>Welcome to GrowSpace</h1>
        <h2>Select a category to explore</h2>

        <!-- Links de navegação dentro de caixas -->
        <div class="menu">
            <a href="coaching.html">Coaching</a>
            <a href="leadership.html">Leadership</a>
            <a href="communication.html">Communication</a>
            <a href="management.html">Management</a>
        </div>

        <!-- Botão de ação -->
        <button>Explore More</button>
    </div>

</body>
</html>
<html>
<head>
<meta charset="utf-8">
<title>index</title>
</head>

<body>
</body>
</html>
